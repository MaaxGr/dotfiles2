#!/bin/bash

# File to write the count to
OUTPUT_FILE="/Users/max/Documents/Ablage/brew-updates-count.txt"

# Get the count of upgradable brew packages
COUNT=$(brew outdated | wc -l | xargs)

echo "$COUNT" > "$OUTPUT_FILE"

echo "Number of brew packages to update: $COUNT (written to $OUTPUT_FILE)" 